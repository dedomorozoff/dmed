name: Release

on:
  push:
    tags:
      - 'v*'

env:
  GOPROXY: https://proxy.golang.org,direct
  GOSUMDB: sum.golang.org

jobs:
  build:
    name: cross-compile (${{ matrix.platform }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux-amd64
          - linux-arm64
          - freebsd-amd64
          - openbsd-amd64
          - netbsd-amd64
          - darwin-amd64
          - darwin-arm64
          - windows-amd64
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build ${{ matrix.platform }}
        run: make build-${{ matrix.platform }} VERSION="${GITHUB_REF_NAME#v}"

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: dmed-${{ matrix.platform }}
          path: dist/dmed-${{ matrix.platform }}*
          if-no-files-found: error

  packages:
    name: package (${{ matrix.target }})
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: deb
            image: ubuntu:24.04
            install: apt-get update && apt-get install -y --no-install-recommends golang-go dpkg-dev
          - target: termux
            image: ubuntu:24.04
            install: apt-get update && apt-get install -y --no-install-recommends golang-go dpkg-dev
          - target: win-zip
            image: ubuntu:24.04
            install: apt-get update && apt-get install -y --no-install-recommends golang-go zip
          - target: rpm
            image: fedora:40
            install: dnf install -y golang rpm-build
          - target: pkg
            image: archlinux:latest
            install: pacman -Syu --noconfirm --needed go base-devel
    container:
      image: ${{ matrix.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Install build deps
        run: ${{ matrix.install }}

      - name: Build package
        if: matrix.target != 'pkg'
        run: make ${{ matrix.target }} VERSION="${GITHUB_REF_NAME#v}"

      # makepkg refuses to run as root; build the Arch package as a regular user.
      - name: Build pacman package (non-root)
        if: matrix.target == 'pkg'
        run: |
          useradd -m builder
          chown -R builder:builder "$GITHUB_WORKSPACE"
          su builder -c "make pkg VERSION=\"${GITHUB_REF_NAME#v}\""

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.target }}
          path: |
            dist/*.deb
            dist/*.rpm
            dist/*.pkg.tar.zst
            dist/*.zip
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: [build, packages]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download binaries and packages
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            dist/* \
            --title "$GITHUB_REF_NAME" \
            --generate-notes